import {describe,it,expect} from "vitest";import type {CommandOutcome,ReadModelPage} from "@wildfire-robotics/api-client/dist/index.js";import {OperatorApiAdapter} from "../src/client";
describe("operator API adapter",()=>{it("maps generated client models without issuing its own HTTP",async()=>{const kinds:string[]=[];const api={list:async(kind:string):Promise<ReadModelPage>=>{kinds.push(kind);return {items:[{id:`${kind}-1`,kind,freshness:{state:kind==="missions"?"stale":"fresh",recordedAt:"2026-07-22T00:00:00Z"},provenance:{producer:"gateway",sourceVersion:"v1",digest:"a".repeat(64)},uncertainty:{description:"bounded"},summary:{count:1}}]}},commandOutcome:async():Promise<CommandOutcome>=>({commandId:"c1",state:"verified",authoritative:true,updatedAt:"2026-07-22T00:00:00Z",correlationId:"r1"})};const adapter=new OperatorApiAdapter({baseUrl:"unused",accessToken:()=>"unused",tenant:"t1",region:"ca-bc",api});const result=await adapter.snapshot(undefined,["c1"]);expect(kinds).toHaveLength(8);expect(result.models.find(m=>m.context==="mission")?.state).toBe("stale");expect(result.commands[0]?.stage).toBe("outcome-confirmed")});});
